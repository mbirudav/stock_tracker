# Environment variables — never commit secrets
.env
.env.local

# SQLite runtime database (created at runtime, persisted via Docker volume)
db/finally.db
db/finally.db-wal
db/finally.db-shm

# Test run artifacts
test/pw-results.txt

# Python bytecode cache
__pycache__/
*.pyc
